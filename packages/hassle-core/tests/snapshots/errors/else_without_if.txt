`with else_then(...):` at test_control_flow_errors.py:42 does not immediately follow an `if_then`/`choose`/`else_if` action in the same action list. HA's `if`/`choose` else-branch is a structural part of that action, so `else_then` must be the very next statement after the `with if_then(...):`/`with choose():` (or `else_if`) block it belongs to. Fix: move `with else_then(...):` directly after its `if`/`choose`, with no other action recorded in between.
