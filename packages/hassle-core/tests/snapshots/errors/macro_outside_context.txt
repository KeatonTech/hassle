`macro notify_all()` was called outside any recording context at bad.py:12. Trigger, condition and action calls only make sense inside an `@automation`/`@script` function body, which the compiler runs inside a recording context. Fix: move this call into a decorated function body; if you meant to build a value, assign the builder to a variable instead of calling `macro notify_all()` at module scope.
