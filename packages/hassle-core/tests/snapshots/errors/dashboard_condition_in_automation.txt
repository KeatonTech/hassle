`cond.state('light.a', 'on')` was passed to an automation condition slot at test_dashboard_errors.py:<line>. The `cond.*` builders make Lovelace CARD-VISIBILITY conditions -- a different schema from Home Assistant's automation conditions, which HA would never evaluate inside an automation, script or `if`/`choose` block. Fix: use the automation vocabulary here (`state(entity).is_("on")`, `numeric_state(entity, above=...)`, `all_of`/`any_of`/`not_`) and keep `cond.*` for `visibility=` and conditional cards inside a `@dashboard` body.
