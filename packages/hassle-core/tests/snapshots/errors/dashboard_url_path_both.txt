`@dashboard(url_path='both-ways', default=True)` at bad.py:6 sets both `url_path=` and `default=True`. THE default dashboard has no `url_path` at all -- HA stores it as `null` and gives it no dashboard-registry item -- so the two are mutually exclusive identities, not a pair of options. Fix: keep exactly one: `url_path='both-ways'` for a sidebar dashboard, or `default=True` for the default one.
