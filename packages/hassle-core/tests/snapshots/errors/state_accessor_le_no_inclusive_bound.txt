`sensor.temp.state <= 21.5` at test_entity_state_conditions.py:176 has no honest native-condition mapping. Home Assistant's `numeric_state` condition only supports EXCLUSIVE bounds (`above`/`below`), not an inclusive `below_or_equal` -- so `<=` cannot be compiled without silently changing behavior right at the boundary value. Fix: use the exclusive `<` operator instead (accepting that the exact boundary value `21.5` is excluded), e.g. `sensor.temp.state < 21.5`, or, if the boundary value itself must be included, pick a value on the correct side of it that is safely outside the entity's real precision (e.g. `< 21.5 - 0.001` for a `<` rewrite of `<=`).
