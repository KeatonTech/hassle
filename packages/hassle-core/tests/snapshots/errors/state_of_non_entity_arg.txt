`expr()`/`state(...).value`/`state_of()` at test_string_state_errors.py:<line>: expected an entity id string or a `state(...)` builder, got 123 (int). Fix: pass a single entity id string (or a `state(...)` builder built from one) -- a template read always names exactly one entity.
