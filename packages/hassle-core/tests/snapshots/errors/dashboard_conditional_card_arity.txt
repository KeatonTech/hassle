`with c.conditional(...):` at test_dashboard_cards_layout.py:<line> holds 2 cards. Its single `card:` key stores exactly one nested card, never a list, so any other count cannot be stored faithfully — HA would either have nothing to render or silently keep only one card. Fix: keep exactly one card directly inside the block (nest a stack card — `with c.vertical_stack():` — to group more than one card there first).
