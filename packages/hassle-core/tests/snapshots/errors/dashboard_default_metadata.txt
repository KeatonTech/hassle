`@dashboard(default=True)` at bad.py:10 also sets `title=`, `icon=`. THE default dashboard has no entry in Home Assistant's dashboard registry -- that registry is what stores a dashboard's title, icon, sidebar visibility and admin-only flag -- so there is nowhere for these to be written and a push would silently drop them. Fix: remove `title=`, `icon=` here (the default dashboard's own sidebar name and icon are HA settings, changed under Settings -> Dashboards), or give this dashboard a `url_path=` so it becomes a real registry-listed dashboard that can carry them.
