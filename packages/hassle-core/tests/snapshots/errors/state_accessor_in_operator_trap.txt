You used Python's `in` operator on `input_select.day_phase.state` at test_entity_state_conditions.py:197. Python's `in` always calls `bool()` on each element comparison to decide membership -- no overload on `input_select.day_phase.state` can intercept that, so `x in [...]` can never build a real Home Assistant condition, only silently evaluate to `True`/`False` once, at compile time. Fix: use `.in_([...])` instead, e.g. `input_select.day_phase.state.in_([...])`, which builds a real `state` condition with list (OR) membership.
