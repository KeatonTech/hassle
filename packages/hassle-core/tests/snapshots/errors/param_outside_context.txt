`param('times')` was called outside any `@shared_script` body at test_templates_macros_errors.py:59. `param()` reads one of the enclosing shared script's own fields at runtime, so it only makes sense inside a function decorated with `@shared_script`. Fix: call `param(...)` only from within a `@shared_script`-decorated function, or use a plain Python value here.
