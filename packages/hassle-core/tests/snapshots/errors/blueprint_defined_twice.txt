The blueprint `automation/local/room-switch-controls.yaml` is declared twice: by a `@blueprint(...)` in the DSL at switches.py:<line>, and by the bundle file `blueprints/automation/local/room-switch-controls.yaml`. Hassle will not pick a winner -- preferring the file would discard whatever the Python says, and preferring the DSL would make compiled output depend on a file's mere existence. Fix: delete whichever one is no longer the source of truth. If you are migrating this blueprint into the DSL, delete the file -- a DSL blueprint needs no file, because the compiled object is what gets pushed.
