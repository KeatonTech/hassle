You used a Python `if`/`bool()` on the runtime state expression `state('binary_sensor.motion')` at test_compile_errors.py:58. Python control flow runs at compile time, so a branch on a live entity state would be evaluated once during compilation and baked in wrong — never re-checked at runtime. Fix: express the branch with a runtime construct, e.g. `with if_then(state('binary_sensor.motion')):` (and `with else_then():` for the else arm), which compiles to HA's `if`/`choose`.
