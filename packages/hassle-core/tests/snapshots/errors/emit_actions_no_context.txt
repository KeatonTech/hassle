`emit_actions` was called outside any recording context at test_capture_actions.py:118. Trigger, condition and action calls only make sense inside an `@automation`/`@script` function body, which the compiler runs inside a recording context. Fix: move this call into a decorated function body; if you meant to build a value, assign the builder to a variable instead of calling `emit_actions` at module scope.
