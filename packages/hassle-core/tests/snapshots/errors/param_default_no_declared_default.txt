`param_default('tag')` at scripts.py:20: `tag` has no declared default -- its `@shared_script` field spec has no `"default"` key (or its signature parameter has no `=...`), so there is no compile-time value for `param_default()` to return. Fix: give `tag` a declared default (a signature default, or a `"default"` key in `fields=`), or use `param('tag')` for the runtime reference instead.
