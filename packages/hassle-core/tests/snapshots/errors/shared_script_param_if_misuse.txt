You used Python's `if/bool()` on the shared-script parameter `tag` at scripts.py:19. Since M19, `tag` inside this body is bound to its runtime `param('tag')` marker (regardless of its declared default), so it has no compile-time value to give `if/bool()` -- it is Jinja text under construction, not a real number/boolean/container, until it renders inside Home Assistant. Fix: if you need a runtime count/condition, use a runtime construct HA itself supports, e.g. `with repeat_count(tag):` (accepts the marker directly and honors whatever the caller passes); if you actually need a compile-time value, `tag` was never a real HA field -- use a module-level constant or a `@macro` argument instead.
