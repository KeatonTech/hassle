`template_sensor(name='Ghost Sensor')` at test_template_helper_dangling_declaration.py:120 has no `state=` and was never used as a decorator -- this declaration does nothing (no `template_sensor` object is built or registered). Fix: either add `state=...` to make this a direct declaration (the call form, e.g. `template_sensor(name='Ghost Sensor', state="{{ ... }}")`), or apply it as a decorator over a zero-arg function, e.g. `@template_sensor(name='Ghost Sensor')` followed by `def ...(): return ...`.
