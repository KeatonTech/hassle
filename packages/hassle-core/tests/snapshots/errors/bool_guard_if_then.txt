`if_then(...)` at test_entity_state_conditions.py:<line> got the plain Python bool `False` where a condition object was expected. A bare `bool` can never be a real Home Assistant condition -- this is almost always the `==`/`!=`-on-a-plain-value mistake (comparing two ordinary Python values instead of building a condition). Fix: build an actual condition, e.g. `entity.state == "on"` / `entity.state.is_not("on")` (the entity-first accessor) or `state(entity_id).is_("on")` (the classic builder), and pass THAT to `if_then(...)` -- never a bare `bool`.
