The raw automation `weird_raw` at test_templates_macros_errors.py:73 contains a value that is not JSON-serializable (Object of type Weird is not JSON serializable). Every HA config is stored as JSON, so a raw body must contain only JSON-compatible values (str/int/float/bool/None/list/dict). Fix: replace the offending value with a JSON-compatible equivalent (e.g. an ISO date string instead of a `datetime`, or a plain number instead of a custom object).
