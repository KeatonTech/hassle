`@dashboard(url_path='climate')` at bad.py:6 has no hyphen in its `url_path`. Home Assistant requires a dashboard's `url_path` to contain a hyphen (its own frontend and backend both enforce it), and Hassle relies on that rule to keep the `default` identity sentinel collision-free. Fix: rename it to a hyphenated slug, e.g. `url_path="climate-dashboard"`.
