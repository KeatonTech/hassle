The `@raw_dashboard(default=True)` body at bad.py:11 returned a non-null `meta` key. THE default dashboard has no entry in Home Assistant's dashboard registry -- that registry is the only thing `meta` describes, and it is what stores a dashboard's title, icon, sidebar visibility and admin-only flag -- so there is nowhere for it to be written and a push would silently drop it. Fix: drop the `"meta"` key from the returned dict (return just the config, or `{"meta": None, "config": {...}}`), or replace `default=True` with `@raw_dashboard(url_path="...")` so this becomes a real registry-listed dashboard that can carry it.
