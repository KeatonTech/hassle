The `sim` fixture could not find a Hassle bundle for test_stray.py: no `hassle.toml` exists in that file's directory or any parent, so there is no bundle to compile. Fix: put the test inside a bundle (a `hassle init` bundle keeps its tests in `<bundle>/tests/`, one level below the `hassle.toml`), or name the bundle explicitly with `@pytest.mark.hassle_bundle("/path/to/bundle")` on the test or module.
