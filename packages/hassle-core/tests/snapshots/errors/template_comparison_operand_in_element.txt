`.in_([..., {'b': 1}, ...])` at test_string_state_errors.py:<line>: every element of `.in_([...])` must be a template expression or a bare int/float/str/bool literal, not dict. Fix: pass only literal values or template expressions as elements (e.g. `.in_(['on', 'off'])`).
