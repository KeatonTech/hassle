The `@raw_dashboard` body for `forgot-return` at bad.py:11 returned a `NoneType`, not a `dict`. A body that ends in an expression rather than a `return` statement returns `None` -- check for a missing `return`. A raw dashboard's body must return either the whole `{"meta": ..., "config": ...}` envelope or just the Lovelace config dict; anything else would be stored as the dashboard's config as-is, so a push would overwrite the real dashboard with it. Fix: `return` a dict from this function -- e.g. `return {"views": [...]}` for a config, or `return {"meta": {"url_path": "..."}, "config": {...}}` for the full envelope. Raw bodies take dicts, never YAML strings (§5.5).
