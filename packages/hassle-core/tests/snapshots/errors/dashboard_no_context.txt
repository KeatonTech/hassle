`raw_card()` was used outside any `@dashboard` body at test_dashboard_errors.py:<line>. Card, view, section and badge verbs record into the dashboard the compiler is currently tracing, and no dashboard is being traced here. Fix: move this call into a function decorated with `@dashboard(url_path="my-dashboard")` (or `@dashboard(default=True)` for the default dashboard); if you meant to build a value rather than record one, assign the dict to a variable instead of calling it at module scope.
