You called Python's `math`/`float()`/`int()` on the runtime expression `{{ x }}` at test_math_expr_errors.py:36. Stdlib `math.*` functions call `float()` on their argument, but a Hassle template expression has no value until it renders inside Home Assistant -- it is Jinja text under construction, not a number. Fix: use Hassle's own math builders instead, e.g. `from hassle.compiler.math_expr import cos` and call `cos({{ x }})`, which stays inside the template and renders to Jinja's `cos(...)`.
