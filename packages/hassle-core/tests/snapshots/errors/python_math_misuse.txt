You called Python's `math`/`float()`/`int()`/`round()`/`math.trunc()` on the runtime expression `{{ x }}` at test_math_expr_errors.py:41. Those stdlib functions call `float()`/`__round__`/`__trunc__` on their argument, but a Hassle template expression has no value until it renders inside Home Assistant -- it is Jinja text under construction, not a number. Fix: use Hassle's own math builders instead, e.g. `from hassle.compiler.math_expr import cos, round_` and call `cos({{ x }})` / `round_({{ x }})`, which stay inside the template and render to Jinja's `cos(...)` / `| round`.
