The blueprint automation declared at bundle.py:4 does not supply the input `room_light`, which `blueprints/automation/local/room-switch-controls.yaml` declares with no `default:` and therefore requires (inputs supplied: `switch_entity`). Real Home Assistant rejects the automation the same way, so the simulator will not guess a value. Fix: add `"room_light": ...` to that automation's `inputs=` dict, or give `room_light` a `default:` in the blueprint's `blueprint.input` block.
