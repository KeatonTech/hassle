`template_number(name='Active HVAC Zones', ...)` at test_template_helper_write_target_error.py:<line> is missing the required `set_value=` keyword. HA's `template` config flow requires a write-target action sequence for a writable `template_number` helper -- `state` alone only computes the displayed value, so the form schema rejects a submission without one. Fix: pass `set_value=` with the action (or list of actions) to run when the helper is written from the UI/a service call, e.g. `template_number(name='Active HVAC Zones', ..., set_value={"action": "input_number.set_value", "target": {"entity_id": "input_number.example"}, "data": {"value": "{{ value }}"}})`.
