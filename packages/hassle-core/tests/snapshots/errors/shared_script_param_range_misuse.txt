You used Python's `range()/int()` on the shared-script parameter `times` at scripts.py:15. Since M19, `times` inside this body is bound to its runtime `param('times')` marker (regardless of its declared default), so it has no compile-time value to give `range()/int()` -- it is Jinja text under construction, not a number or a boolean, until it renders inside Home Assistant. Fix: for deliberate compile-time metaprogramming (e.g. unrolling a `for _ in range(...):` loop a fixed number of times), use `param_default('times')` instead -- it returns `times`'s DECLARED default, a plain Python value, not the runtime marker.
