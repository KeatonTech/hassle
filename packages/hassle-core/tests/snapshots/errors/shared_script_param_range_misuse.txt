You used Python's `range()/int()` on the shared-script parameter `times` at scripts.py:20. Inside a `@shared_script` body, `times` is bound to its runtime `param('times')` marker (regardless of its declared default), so it has no compile-time value to give `range()/int()` -- it is Jinja text under construction, not a real number/boolean/container, until it renders inside Home Assistant. Fix: if you need a runtime count/condition, use a runtime construct HA itself supports, e.g. `with repeat_count(times):` (accepts the marker directly and honors whatever the caller passes); if you actually need a compile-time value, `times` was never a real HA field -- use a module-level constant or a `@macro` argument instead.
