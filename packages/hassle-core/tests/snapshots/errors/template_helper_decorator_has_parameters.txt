`@template_sensor(name='Bad Sensor', ...)` at test_template_helper_decorator_form.py:165: the decorated function `_bad_sensor` takes parameter(s) (some_arg), but a template-helper decorator body is always called with zero arguments. Fix: the decorated function must take no parameters and `return` a template expression built from the `hassle.compiler.templates`/`hassle.compiler.math_expr` surface (e.g. `return expr('sensor.x') + 1`) or a plain Jinja string -- no service calls, no other side effects, exactly one `return`ed value.
