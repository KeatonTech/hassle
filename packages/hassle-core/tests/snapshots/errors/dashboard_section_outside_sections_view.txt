`section()` was used inside a `masonry` view at test_dashboard_errors.py:<line>. HA stores a `sections` list only on a view whose `type` is `sections`; every other view type stores one flat `cards` list, and sections never nest inside each other or inside a card. Fix: open the enclosing view as `view(type="sections")` (the authoring default) and put the `with section():` directly inside it -- or drop the section here and record the cards directly, grouping them with a stack card if you wanted the visual box.
