A card was recorded directly inside a `sections` view at test_dashboard_errors.py:<line>. A `sections` view stores only sections -- the cards live inside them -- so a card here has nowhere to be stored and would be silently dropped. Fix: wrap it in `with section():` (grouping related cards into one grid section), or open the enclosing view as `view(type="masonry")`, the free-form column layout that does take cards directly.
