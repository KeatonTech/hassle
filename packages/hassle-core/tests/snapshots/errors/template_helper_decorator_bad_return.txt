`@template_sensor(name='Bad Return Sensor', ...)` at test_template_helper_decorator_form.py:188: the decorated function `_bad_return_sensor` returned 42 (int), not a `TemplateExpr`/`str`. Fix: the decorated function must take no parameters and `return` a template expression built from the `hassle.compiler.templates`/`hassle.compiler.math_expr` surface (e.g. `return expr('sensor.x') + 1`) or a plain Jinja string -- no service calls, no other side effects, exactly one `return`ed value.
