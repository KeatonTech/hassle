Helper `input_boolean:guest_mode` declares `id="guest_mode"`, but Home Assistant derives a helper's real identity by slugifying its `name` ("Guest Flag" -> `guest_flag`), ignoring the supplied id. at automation.py:4 Fix: Change `id="guest_mode"` to `id="guest_flag"` (or rename the helper to a `name` that slugifies to `guest_mode`), so the bundle's id matches what HA will actually assign.
