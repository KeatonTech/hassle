Helper `input_boolean:guest_room_mode` declares `id="guest_room_mode"`, but `input_boolean.guest_room_mode` is not in the registry snapshot -- Home Assistant will create it fresh via the WS API, which derives a new helper's real identity by slugifying its `name` ("Guest Room Flag" -> `guest_room_flag`), ignoring the supplied id. at automation.py:4 Fix: Change `id="guest_room_mode"` to `id="guest_room_flag"` (or rename the helper to a `name` that slugifies to `guest_room_mode`), so the bundle's id matches what HA will actually assign. (This only applies to new helpers Hassle creates; an already-existing helper with this id is exempt -- see docs/ha-api-notes.md §17.5.)
