`CATEGORY = "Totally Different Name"` in `automatic_hvac.py` does not slugify to this file's own name -- `slugify("Totally Different Name")` is `totally_different_name`, but the file is named `automatic_hvac.py`. at automatic_hvac.py:4 Fix: Rename the file to `totally_different_name.py` (or change `CATEGORY` to a name that slugifies to `automatic_hvac`, e.g. a display name close to `Automatic Hvac`) so the declared name and the file's placement agree on which HA category this is. Until fixed, `hassle push` ignores `CATEGORY` for this file and falls back to a slug-derived name if it has to create the category.
