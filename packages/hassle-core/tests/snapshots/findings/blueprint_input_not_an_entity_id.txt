`automation:office_switch` supplies `kitchen` for the blueprint input `room_light`, which `blueprints/automation/local/room-switch-controls.yaml` declares as an entity selector, but that is not an entity id (Home Assistant entity ids are `<domain>.<object_id>`, e.g. `light.kitchen`) at misc.py:<line> Fix: replace it with the full entity id, e.g. `"room_light": "light.kitchen"` if that is the entity you meant. Home Assistant accepts the save and then silently never matches the entity, so this one does not even fail loudly at push time.
