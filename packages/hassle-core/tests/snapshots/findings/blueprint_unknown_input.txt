`automation:office_switch` supplies the blueprint input `romo_light`, which `blueprints/automation/local/room-switch-controls.yaml` does not declare (it declares: `dim_step_pct`, `room_light`, `switch_entity`) at misc.py:<line> Fix: remove `romo_light` from this automation's `inputs=` dict, or add it to the blueprint's `blueprint.input` block. Did you mean `room_light`? Home Assistant rejects an unknown input the same way at save time.
