`automation:office_switch` does not supply the blueprint input `room_light`, which `blueprints/automation/local/room-switch-controls.yaml` declares with no `default:` and therefore requires at misc.py:<line> Fix: add `"room_light": ...` to this automation's `inputs=` dict, or give `room_light` a `default:` in the blueprint's `blueprint.input` block. Home Assistant rejects the automation the same way at save time, with an opaque HTTP 400.
