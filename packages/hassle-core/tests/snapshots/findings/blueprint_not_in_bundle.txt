`automation:office_switch` uses the blueprint `jay-kub/taps.yaml`, which this bundle does not contain -- so Hassle cannot push it, validate this automation's inputs against it, or simulate the automation at all. That is fine if it is a community blueprint you imported into Home Assistant by hand (it lives only there, and Home Assistant cannot serve its source back) at misc.py:<line> Fix: save the blueprint's YAML into the bundle at `blueprints/automation/jay-kub/taps.yaml` to bring it under management -- Hassle will then push it before this automation, check this automation's inputs offline, and expand it in tests. Leave it as it is to keep referencing Home Assistant's own copy.
