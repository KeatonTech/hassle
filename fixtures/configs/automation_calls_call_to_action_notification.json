{
  "id": "garage_door_opened_notify",
  "alias": "Garage door opened notify",
  "description": "Shape observed in a real UI-authored automation (ux/shared-script-rich-fields, owner feedback): calls a managed script whose every field carries selector/name/description metadata (companion fixture script_call_to_action_notification.json), with data matching every declared field and the metadata:{} every UI-saved action carries.",
  "triggers": [
    {
      "trigger": "state",
      "entity_id": ["cover.garage_door"],
      "to": ["open"]
    }
  ],
  "conditions": [],
  "actions": [
    {
      "action": "script.call_to_action_notification",
      "data": {
        "title": "Garage",
        "message": "Door opened",
        "action_button": "view",
        "action_button_icon": "mdi:garage-open",
        "tag": "garage_door"
      },
      "metadata": {}
    }
  ],
  "mode": "single"
}
